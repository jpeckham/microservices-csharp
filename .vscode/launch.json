{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Everything (Docker Compose)",
      "type": "node-terminal",
      "request": "launch",
      "command": "docker compose up --build",
      "cwd": "${workspaceFolder}",
      "postDebugTask": "docker-compose-down",
      "serverReadyAction": {
        "pattern": "social-web.*Now listening on",
        "uriFormat": "http://localhost:32123",
        "action": "openExternally"
      }
    }
  ]
}
